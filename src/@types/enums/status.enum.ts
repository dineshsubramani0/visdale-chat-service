export enum Status {
  active = 'active',
  inactive = 'inactive',
  enabled = 'enabled',
  disabled = 'disabled',
}

export enum UserStatus {
  PENDING = 'PENDING',
  VERIFIED = 'VERIFIED',
  CREATED = 'CREATED',
}
